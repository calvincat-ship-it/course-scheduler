# F. 導師線上填課協作 — 設計文件

> ROADMAP 項目 F。使用者 2026-08-07 提出的願景。**設計先行**；本檔為定案設計，實作時逐塊對照。
> 三塊：① 自編課程（排課）→ ② 鎖定 → ③ 導師線上填課。

## 已定案決策（2026-08-07）

1. **開發順序**：先做 ①自編 + ②鎖定（本機、免後端、立即可用、且是 ③ 的前提）；③ 導師線上填課另立一階段。
2. **③ 協作機制**：**免後端**。導師用學校 `@ttct.edu.tw` Google 登入，每班一個共享 Drive 檔、只能編自己班。無成本、無伺服器、真實身分。代價：每位導師首次需一次 Google 授權。
3. **導師「填課」語意**：**只填「已排好的自編格」的內容**（排課人員先決定自編區塊排在哪幾節，導師只在那些格填內容名稱，如「自主學習／閱讀／社團」）。導師不決定位置、不新增節數 → 不會破壞排課、不會超節/衝堂。

---

## ① 自編課程（本機排課功能）

### 概念
級任導師可自主運用的區塊（彈性／自主學習／導師時間之類）。排課人員在盤面上把這些「自編節」排好位置，內容留待導師填。

### 資料模型
- 科目層新增旗標 `subject.selfDesigned: true`。此類科目：
  - **老師 = 該班級任導師**（由 `teacher.homeroomClassId` 反查），**不需教師配課**（`teacher.load` 不必有列）。
  - **不受一般約束**：不計教師/教室衝堂、不受科目型態（分組/分節/協同）與節數硬檢核約束。
  - 「視為同一課程」：一個班的所有自編節是**可互換的區塊池**（共 N 節），排課時當成同一種課來擺放。
- 每個已排自編格的內容存 `state.slotContent['classId|day|period'] = '文字'`（導師之後填；預設空）。

### 排課行為（接 app.js 排課引擎）
- `canPlaceAt()`：自編科目**略過**教師衝堂/教室/型態檢查（導師教自己班、單一）；仍尊重「該班該節有課」與該班該格未被占用。
- `runAutoSchedule()`：自編節**優先湊 2–3 節連堂**放入（連續節次成塊），放不下再拆散；不與其他課製造衝堂。
- `placeWithExtras()` / 手排：自編科目放課時直接記 `slots[key]=selfSubjId`，老師隱含為導師，不動 `slotTeachers`。

### UI
- ① 科目編輯 modal 增「自編課程」選項（與現有教學型態並列或獨立勾選）。
- 盤面自編格樣式可與一般課區隔（如虛線框），格內顯示 `slotContent` 或「（待導師填）」。

### 待決細節
- 自編科目節數怎麼進「②年級 科目節數」與 Σ 檢核？建議：自編節仍計入該年級「可用格數」的占用，但**不強制 Σ==**（自編是彈性）。→ 實作時定。
- 一校一個「自編課程」科目即可，或允許多個（自主學習、社團…各一）？建議先**單一自編科目**，內容用 `slotContent` 區分。

---

## ② 鎖定（本機）

### 資料模型
- `state.locked: true`（可加 `lockedAt`）。

### 行為
- 鎖定後：**所有非自編格唯讀**（不能放/移除/自動排課）；**自編格仍可編其內容**（排課人員本機也能代填）。
- 排課人員可「解除鎖定」回到自由編輯。
- ⑤ 排課頁顯示 🔒 鎖定橫幅 + 鎖定/解除按鈕。

### 用途
- 本機收尾防手滑；也是 ③ 開放線上填課的前置狀態（鎖定 = 課表定稿）。

---

## ③ 導師線上填課（未來階段，免後端 / Google 登入）

> 本段為方向設計，實作前需再細化（scope 選擇、資料夾分享 UX、Google Picker）。

### 為何一定要導師登入
要讓導師的瀏覽器把內容寫回雲端，必須有「某人的 Google 權杖」。無後端下只能是**導師自己登入**；純輸入姓名無法取得寫入權限（除非架後端）。→ 已決定走 Google 登入。

### 架構
- **每班一檔、天生無併發**：一位導師只編自己班 → 各寫各的檔，不會互相覆蓋。
- 排課人員 App「開放線上填課」時：
  1. 在自己 Drive 建一個共享資料夾（如 `課務-自編填課-113`），放 `master.json`（鎖定後的課表，唯讀參考）。
  2. 每班一個 `class-<id>.json`（該班自編格清單 + 內容）。
  3. 把資料夾分享給各導師的 `@ttct.edu.tw`（由教師名單 email，或網域內共用），可編輯。
- 導師流程：開連結 → Google 登入（學校帳號）→ App 讀 `master`，以**登入 email 對應教師記錄**（對不到時請導師從名單**選自己姓名** = 「輸入姓名取權限」的落地）→ 解析其 `homeroomClassId` → 載入該班自編格 + 現有內容 → 逐格填內容 → 存回 `class-<id>.json`。
- 排課人員 App「收回填課」：拉所有 `class-<id>.json`，把內容 merge 進 `state.slotContent`，顯示變更摘要。

### OAuth scope
- 目前雲端備份用 `drive.appdata`（私有、不可分享）**不適用**。③ 需 `drive.file`（App 經 Picker/建立取得的檔）或 `drive`（全權，consent 摩擦大、學校可能限制）。
- 傾向 `drive.file` + Google Picker 讓導師開共享檔；細節實作前再定。

### 安全/信任模型
- 校內信任情境；非真正驗證。防護點：需 Google 登入（非匿名）、編輯權限**以檔案分享逐班限定**、內容僅自編格。導師「選姓名」是 UX 對應、非安全邊界。→ 可接受，但實作時於 UI 明示。

### 待決
- scope 最終選 `drive.file` vs `drive`；資料夾/檔案分享是逐位 email 還是網域共用；Picker 流程；「收回填課」的衝突與變更呈現。

---

## 資料模型異動彙總
- `subject.selfDesigned: boolean`（①）
- `state.slotContent: { 'classId|day|period': string }`（①，導師填的內容）
- `state.locked: boolean`（+ `lockedAt?`）（②）
- ③ 另需：Drive 共享資料夾 / 每班檔（不進本機 state schema，屬雲端層）

## 風險/備註
- ① 動到排課引擎核心（`canPlaceAt`/`runAutoSchedule`/`placeWithExtras`），需回歸測試既有排課不受影響。
- schema 仍為 2、以上皆為新增欄位、向下相容（舊資料 guard 補預設）。
- ③ 不在本次；本檔保存其設計，避免遺失。
