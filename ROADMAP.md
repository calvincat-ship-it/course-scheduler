# 課務編排 — 開發待辦 (ROADMAP)

> 使用者要「之後逐次開發」的功能清單。開發時從這裡挑一項，做完把它移到下方「已完成」並更新 HANDOFF。

## 待開發（依建議整理，未定先後）

### A. 半自動排課建議（手排輔助）— ✅ 已完成（v05.00）
- 見「已完成」區。後續可再加：跨班調課連鎖（目前只在同班內搬移）、支援分組/協同/分節/連堂科目的調課建議（目前限單純科目）、教師視角「哪幾格可調」總覽。

### B. Google Drive 雲端同步（個人多裝置備份）— ✅ 已完成（v07.00）
- 見「已完成」區。**上線前置**：需在 Google Cloud Console 建本 App 專屬 OAuth 用戶端，把 client ID 填入 `GOOGLE_CLIENT_ID`（現為空＝顯示「尚未設定」）。
- 註：此為「排課人員自己」的多裝置備份（appDataFolder 私有、不可分享）。**導師線上填課協作屬另一件事，見下方 F。**

### F. 導師線上填課協作（未來大功能，使用者 2026-08-07 提出的願景，尚未動工）
分三塊，依序做：
1. **自編課程標記**：級任導師的課程可標為「自編」；自編課視為同一課程、**不受課程設定（節數/型態等）約束**，排課時儘量以 **2–3 堂連堂**方式排。（v05.01 教師 `homeroomClassId` 即為此鋪路）
   - ✅ **①a 已完成（v08.00）**：自編旗標＋手動擺放（引擎 bypass、免配課、不擋排課、自動排課保留自編格）＋每格內容 `slotContent`＋盤面/調色盤/內容 modal。
   - ⬜ **①b 未做**：自動排課把自編排成 2–3 連堂（目前自編為手動擺放，自動排課不動它）。
2. **鎖定**：排課完成後，排課人員把「不開放調整」的資料鎖定（唯讀），只留自編格可編。
3. **導師線上填課**：開放後，級任導師**輸入自己姓名取得「自編權限」**，只能填自己班的自編課程。
- **關鍵架構限制**：appDataFolder 是「每帳號私有、不可分享」，**無法**給導師存取。此協作需另一種機制：共享 Drive 檔／共享連結，或改用有後端服務；並要處理多導師併發（latest-wins 或各班分檔合併）與「姓名取權限」的信任模型（校內信任即可，但非真正驗證）。動工前需專門設計一輪。

### C. 各年級「領域節數參考表」— ✅ 已完成（v06.00）
- 見「已完成」區。

### D. 全校總表輸出強化 / PNG 圖示
- 全校一覽總表（跨班/跨師的大表）輸出。
- PWA 圖示補 PNG（目前只有 SVG，安裝可用但部分平台偏好 PNG）。
- docx 班級表右側 legend「領域合併」（如 健康+體育→健體、音樂+美勞→藝文），目前是逐科各列一行。

### E. App 內自動排課 — 進階最佳化（v04.00 基礎版、v04.01 進階版，見已完成區；以下為仍未做的）
- ✅ 已做（v04.01）：分散不同天/隔天、教師單日上限（可個別覆寫、科目可豁免）、教師每日平衡、上午避免湊滿、偏好時段（上午/下午）、隨機重啟求解取較佳解。
- 仍**未納入**（需要時再做）：
  - 第七節排輕科／級任課的規則（需「節次×科目類型」層級設定）。
  - 社會/自然「2 連堂＋1 獨立且不相鄰」這類 pattern 約束。
  - 「偏好某指定節」（如國語偏好第1節，目前只有上午/下午 band）。
  - 更強求解：回溯／局部搜尋，進一步提高高難度情形的填滿率（目前為隨機重啟貪婪）。

## 已完成（封存，供參考）
- **v07.00 Google Drive 雲端同步（個人多裝置備份）**：設定頁「☁️ 雲端同步」連結 Google→整份 state 自動備份到自己雲端硬碟 appDataFolder（無伺服器、debounce 8s、save() choke point 觸發）；立即備份、多裝置開 App 較新即詢問還原、還原版本選擇器（最新＋每日歷史最多 7）、跨裝置衝突保留 .prev、更換帳號隔離、解除連結。移植血壓 App 的 GIS token 模型＋Drive REST。**待填 `GOOGLE_CLIENT_ID`（本 App 專屬 OAuth 用戶端）才會實際啟用**；appDataFolder 每個 OAuth 用戶端各自獨立，不可與血壓/記事本共用。
- **v06.00 領域節數參考表**：新增「領域節數」分頁＝①可編輯的建議節數參考表（領域×一~六年級，內建 108 課綱國小起始值、務必校對）＋②各年級「實配 / 建議」對照矩陣（相符綠、不符紅底）。①科目加「所屬領域」下拉；state.domains＋subject.domainId。**待使用者校對 108 課綱數字**（尤其五六年級英語/綜合、彈性學習）。
- **v05.00 半自動排課建議（手排輔助）**：⑤排課①未選科目點空格→「空格建議」列出可放的(科,師)一鍵放入；②排不下的單純科目調色盤出現「🔧 喬課」→ findEvictionChain 同班內回溯搜尋（≤3步、狀態不變、operations budget）給多步調課連鎖、一鍵套用保證 0 衝堂。底層沿用 canPlaceAt（順手修 day 字串/數字正規化 bug）。限單純科目（非分組/分節/協同/連堂）。
- **v04.01 自動排課進階**：硬約束＋分散不同天/隔天、教師單日節數上限（全域＋個別覆寫＋科目豁免 excludeDailyCap）；軟性偏好＋教師每日節數平衡、上午避免湊滿、科目偏好時段（上午/下午）、同科分散；求解改隨機重啟（多趟取「排最多、其次品質分最低」）。科目/教師/設定新增對應欄位。
- **v04.00 通用自動填課引擎（選用）**：⑤排課「🪄 自動排課」按鈕；靜態緊度排序＋貪婪 MRV，尊重所有硬約束（教師/教室衝堂、不排課、協同同步、連堂、分節、年級節次、每科填滿）＋科目排課限制；清空重排/只補空格；排不下列表回報；不製造衝堂。搭配科目設定新增「🔒 排課限制」（限定上課日／節次白名單）。
- v02.01 教師配課下拉：排除已配滿科目、顯示剩餘節數、不得超過剩餘。
- v02.02 分節上課：第三種科目模式，多師分攤節數各上不同節（slotTeachers）。
- v03.00 一鍵輸出 .docx：所有班級/教師課表，版面比照三民國小範本。
- 外部 CP-SAT 自動填課流程：`tools/auto_schedule.py` + `verify` + `render`，規則見 `排課規則.md`（三民國小專屬）。
