# 課務編排 — 開發待辦 (ROADMAP)

> 使用者要「之後逐次開發」的功能清單。開發時從這裡挑一項，做完把它移到下方「已完成」並更新 HANDOFF。

## 待開發（依建議整理，未定先後）

### A. 半自動排課建議（手排輔助）
- 某個空格「可以放哪些科目/老師」提示（掃描不違反硬性衝堂的候選）。
- 某位老師「哪幾格可以調動」提示。
- 三角/多角調課建議（A↔B↔C 換課解卡）：當某格排不下時，自動找出「移動哪幾堂能騰出空間」的連鎖建議。
- 定位：不自動排滿，而是在使用者手排卡住時給提示，保留人工決策。中等工程量。

### B. Google Drive 雲端同步
- 比照血壓 App / 智慧記事本：Drive appDataFolder 自動備份 + 多裝置接續 + 還原選擇器。
- 現況資料只在單機 IndexedDB（單一 key `state`），換裝置要靠「匯出/匯入 JSON」。
- 資料層已是單一 state 文件，接起來相對容易；GIS token 模型可沿用其他 App 經驗。

### C. 各年級「領域節數參考表」
- 各年級領域（如健體、藝文、綜合…）建議節數的參考對照表，配課/年級節數設定時可對照。
- 註：v02.01 已做「教師配課下拉顯示剩餘節數」；此項是另外的「領域層級」參考表。

### D. 全校總表輸出強化 / PNG 圖示
- 全校一覽總表（跨班/跨師的大表）輸出。
- PWA 圖示補 PNG（目前只有 SVG，安裝可用但部分平台偏好 PNG）。
- docx 班級表右側 legend「領域合併」（如 健康+體育→健體、音樂+美勞→藝文），目前是逐科各列一行。

### E.（進行中）把自動排課整進 App — 選用功能
- 見下方「進行中」。

## 進行中
- **App 內選用自動排課**：把外部 CP-SAT 流程的精神帶進 App，做成使用者可選用的一鍵/半自動填課。範圍與做法開發時與使用者確認（外部 CP-SAT 為三民國小專屬最佳化，App 內版本走通用約束）。

## 已完成（封存，供參考）
- v02.01 教師配課下拉：排除已配滿科目、顯示剩餘節數、不得超過剩餘。
- v02.02 分節上課：第三種科目模式，多師分攤節數各上不同節（slotTeachers）。
- v03.00 一鍵輸出 .docx：所有班級/教師課表，版面比照三民國小範本。
- 外部 CP-SAT 自動填課流程：`tools/auto_schedule.py` + `verify` + `render`，規則見 `排課規則.md`（三民國小專屬）。
